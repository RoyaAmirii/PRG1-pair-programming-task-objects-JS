function ispalindrome (string) {
    const reversestring = string.split ('') .reverse() .join('') //this is not where the testing occurs
    return string == reversestring //the double equals checks if they're different/does the testing
    if (string == reverse) {
        return (word=== reverse)


        //return true
        //else 
          //  return false
    }

}
